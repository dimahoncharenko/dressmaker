generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum DecalType {
  LOGO
  TEXTURE
}

model Decal {
  id    Int       @id @default(autoincrement())
  type  DecalType
  title String
  image String
}
